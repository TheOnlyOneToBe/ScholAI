{% extends 'base.html.twig' %}

{% block title %}Payementmethod{% endblock %}

{% block body %}
    <h1>Payementmethod</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ payementmethod.id }}</td>
            </tr>
            <tr>
                <th>PayementName</th>
                <td>{{ payementmethod.PayementName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_payementmethod_index') }}">back to list</a>

    <a href="{{ path('app_payementmethod_edit', {'id': payementmethod.id}) }}">edit</a>

    {{ include('payementmethod/_delete_form.html.twig') }}
{% endblock %}
