{% extends 'base.html.twig' %}

{% block title %}Bourse{% endblock %}

{% block body %}
    <h1>Bourse</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bourse.id }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ bourse.montant }}</td>
            </tr>
            <tr>
                <th>Remise</th>
                <td>{{ bourse.remise }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_bourse_index') }}">back to list</a>

    <a href="{{ path('app_bourse_edit', {'id': bourse.id}) }}">edit</a>

    {{ include('bourse/_delete_form.html.twig') }}
{% endblock %}
