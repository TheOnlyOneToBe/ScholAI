{% extends 'base.html.twig' %}

{% block title %}Edit Bourse{% endblock %}

{% block body %}
    <h1>Edit Bourse</h1>

    {{ include('bourse/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_bourse_index') }}">back to list</a>

    {{ include('bourse/_delete_form.html.twig') }}
{% endblock %}
