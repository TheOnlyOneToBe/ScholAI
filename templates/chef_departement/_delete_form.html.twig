<form method="post" action="{{ path('app_chef_departement_delete', {'id': chef_departement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ chef_departement.id) }}">
    <button class="btn">Delete</button>
</form>
