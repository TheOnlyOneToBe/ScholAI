{% extends 'base.html.twig' %}

{% block title %}Reglement index{% endblock %}

{% block body %}
    <h1>Reglement index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Montant_reglee</th>
                <th>Datereglement</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for reglement in reglements %}
            <tr>
                <td>{{ reglement.id }}</td>
                <td>{{ reglement.montantReglee }}</td>
                <td>{{ reglement.datereglement ? reglement.datereglement|date('Y-m-d') : '' }}</td>
                <td>
                    <a href="{{ path('app_reglement_show', {'id': reglement.id}) }}">show</a>
                    <a href="{{ path('app_reglement_edit', {'id': reglement.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_reglement_new') }}">Create new</a>
{% endblock %}
