<form method="post" action="{{ path('app_impression_format_delete', {'id': impression_format.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ impression_format.id) }}">
    <button class="btn">Delete</button>
</form>
