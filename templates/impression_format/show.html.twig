{% extends 'base.html.twig' %}

{% block title %}ImpressionFormat{% endblock %}

{% block body %}
    <h1>ImpressionFormat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ impression_format.id }}</td>
            </tr>
            <tr>
                <th>NomFormat</th>
                <td>{{ impression_format.nomFormat }}</td>
            </tr>
            <tr>
                <th>LargeurPage</th>
                <td>{{ impression_format.largeurPage }}</td>
            </tr>
            <tr>
                <th>LongueurPage</th>
                <td>{{ impression_format.longueurPage }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ impression_format.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_impression_format_index') }}">back to list</a>

    <a href="{{ path('app_impression_format_edit', {'id': impression_format.id}) }}">edit</a>

    {{ include('impression_format/_delete_form.html.twig') }}
{% endblock %}
