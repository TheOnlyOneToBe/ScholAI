{{ form_start(delete_form, {'attr': {'class': 'mt-6', 'onsubmit': 'return confirm("Êtes-vous sûr de vouloir supprimer ce cycle ?");'}}) }}
    <button class="bg-red-500 hover:bg-red-600 text-white font-bold py-2 px-4 rounded inline-flex items-center">
        <i class="fas fa-trash-alt mr-2"></i>Supprimer
    </button>
{{ form_end(delete_form) }}
