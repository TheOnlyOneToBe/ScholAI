{% extends 'base.html.twig' %}

{% block title %}FiliereCycle{% endblock %}

{% block body %}
    <h1>FiliereCycle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ filiere_cycle.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ filiere_cycle.description }}</td>
            </tr>
            <tr>
                <th>FraisInscription</th>
                <td>{{ filiere_cycle.fraisInscription }}</td>
            </tr>
            <tr>
                <th>MontantPension</th>
                <td>{{ filiere_cycle.montantPension }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_filiere_cycle_index') }}">back to list</a>

    <a href="{{ path('app_filiere_cycle_edit', {'id': filiere_cycle.id}) }}">edit</a>

    {{ include('filiere_cycle/_delete_form.html.twig') }}
{% endblock %}
