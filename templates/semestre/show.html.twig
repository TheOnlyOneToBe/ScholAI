{% extends 'base.html.twig' %}

{% block title %}Semestre{% endblock %}

{% block body %}
    <h1>Semestre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ semestre.id }}</td>
            </tr>
            <tr>
                <th>Nomsemestre</th>
                <td>{{ semestre.nomsemestre }}</td>
            </tr>
            <tr>
                <th>Datedebut</th>
                <td>{{ semestre.datedebut ? semestre.datedebut|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Datefin</th>
                <td>{{ semestre.datefin ? semestre.datefin|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_semestre_index') }}">back to list</a>

    <a href="{{ path('app_semestre_edit', {'id': semestre.id}) }}">edit</a>

    {{ include('semestre/_delete_form.html.twig') }}
{% endblock %}
