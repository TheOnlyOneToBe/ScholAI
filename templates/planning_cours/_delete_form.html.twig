<form method="post" action="{{ path('app_planning_cours_delete', {'id': planning_cour.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ planning_cour.id) }}">
    <button class="btn">Delete</button>
</form>
