{% extends 'base.html.twig' %}

{% block title %}Payementreason index{% endblock %}

{% block body %}
    <h1>Payementreason index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Raison</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for payementreason in payementreasons %}
            <tr>
                <td>{{ payementreason.id }}</td>
                <td>{{ payementreason.Raison }}</td>
                <td>
                    <a href="{{ path('app_payementreason_show', {'id': payementreason.id}) }}">show</a>
                    <a href="{{ path('app_payementreason_edit', {'id': payementreason.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_payementreason_new') }}">Create new</a>
{% endblock %}
