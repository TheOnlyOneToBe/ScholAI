<form method="post" action="{{ path('app_payementreason_delete', {'id': payementreason.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ payementreason.id) }}">
    <button class="btn">Delete</button>
</form>
