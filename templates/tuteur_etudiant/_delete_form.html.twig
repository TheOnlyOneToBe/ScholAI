<form method="post" action="{{ path('app_tuteur_etudiant_delete', {'id': tuteur_etudiant.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tuteur_etudiant.id) }}">
    <button class="btn">Delete</button>
</form>
