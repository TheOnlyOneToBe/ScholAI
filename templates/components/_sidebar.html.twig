<!-- Sidebar -->
<div class="flex min-h-0 flex-1 flex-col bg-white">
    <!-- Logo -->
    <div class="flex h-16 flex-shrink-0 items-center border-b border-gray-200 px-6">
        <span class="text-2xl font-bold text-indigo-600">SCHOL</span>
    </div>

    <!-- Navigation -->
    <div class="flex flex-1 flex-col overflow-y-auto">
        <nav class="flex-1 space-y-1 px-4 py-4">
            <!-- Dashboard -->
            <a href="{{ path('app_home') }}" 
               class="group flex items-center rounded-md px-2 py-2 text-sm font-medium {% if app.request.get('_route') == 'app_home' %}bg-indigo-100 text-indigo-600{% else %}text-gray-600 hover:bg-gray-50 hover:text-gray-900{% endif %}">
                <i class="fas fa-home mr-3 h-6 w-6"></i>
                Tableau de bord
            </a>

            <!-- Gestion Académique -->
            <div class="mt-4">
                <p class="px-2 text-xs font-semibold uppercase tracking-wider text-gray-500">
                    Gestion Académique
                </p>
                <div class="mt-2 space-y-1">
                    <!-- Étudiants -->
                    <a href="{{ path('app_etudiant_index') }}" 
                       class="group flex items-center rounded-md px-2 py-2 text-sm font-medium {% if app.request.get('_route') starts with 'app_etudiant' %}bg-indigo-100 text-indigo-600{% else %}text-gray-600 hover:bg-gray-50 hover:text-gray-900{% endif %}">
                        <i class="fas fa-user-graduate mr-3 h-6 w-6"></i>
                        Étudiants
                    </a>

                    <!-- Professeurs -->
                    <a href="{{ path('app_professeur_index') }}" 
                       class="group flex items-center rounded-md px-2 py-2 text-sm font-medium {% if app.request.get('_route') starts with 'app_professeur' %}bg-indigo-100 text-indigo-600{% else %}text-gray-600 hover:bg-gray-50 hover:text-gray-900{% endif %}">
                        <i class="fas fa-chalkboard-teacher mr-3 h-6 w-6"></i>
                        Professeurs
                    </a>

                    <!-- Notes -->
                    <a href="{{ path('app_note_index') }}" 
                       class="group flex items-center rounded-md px-2 py-2 text-sm font-medium {% if app.request.get('_route') starts with 'app_note' %}bg-indigo-100 text-indigo-600{% else %}text-gray-600 hover:bg-gray-50 hover:text-gray-900{% endif %}">
                        <i class="fas fa-star mr-3 h-6 w-6"></i>
                        Notes
                    </a>

                    <!-- Évaluations -->
                    <a href="{{ path('app_evaluation_index') }}" 
                       class="group flex items-center rounded-md px-2 py-2 text-sm font-medium {% if app.request.get('_route') starts with 'app_evaluation' %}bg-indigo-100 text-indigo-600{% else %}text-gray-600 hover:bg-gray-50 hover:text-gray-900{% endif %}">
                        <i class="fas fa-tasks mr-3 h-6 w-6"></i>
                        Évaluations
                    </a>

                    <!-- Matières -->
                    <a href="{{ path('app_matiere_index') }}" 
                       class="group flex items-center rounded-md px-2 py-2 text-sm font-medium {% if app.request.get('_route') starts with 'app_matiere' %}bg-indigo-100 text-indigo-600{% else %}text-gray-600 hover:bg-gray-50 hover:text-gray-900{% endif %}">
                        <i class="fas fa-book mr-3 h-6 w-6"></i>
                        Matières
                    </a>
                </div>
            </div>

            <!-- Organisation -->
            <div class="mt-4">
                <p class="px-2 text-xs font-semibold uppercase tracking-wider text-gray-500">
                    Organisation
                </p>
                <div class="mt-2 space-y-1">
                    <!-- Campus -->
                    <a href="{{ path('app_campus_index') }}" 
                       class="group flex items-center rounded-md px-2 py-2 text-sm font-medium {% if app.request.get('_route') starts with 'app_campus' %}bg-indigo-100 text-indigo-600{% else %}text-gray-600 hover:bg-gray-50 hover:text-gray-900{% endif %}">
                        <i class="fas fa-university mr-3 h-6 w-6"></i>
                        Campus
                    </a>

                    <!-- Départements -->
                    <a href="{{ path('app_departement_index') }}" 
                       class="group flex items-center rounded-md px-2 py-2 text-sm font-medium {% if app.request.get('_route') starts with 'app_departement' %}bg-indigo-100 text-indigo-600{% else %}text-gray-600 hover:bg-gray-50 hover:text-gray-900{% endif %}">
                        <i class="fas fa-building mr-3 h-6 w-6"></i>
                        Départements
                    </a>

                    <!-- Filières -->
                    <a href="{{ path('app_filiere_index') }}" 
                       class="group flex items-center rounded-md px-2 py-2 text-sm font-medium {% if app.request.get('_route') starts with 'app_filiere' %}bg-indigo-100 text-indigo-600{% else %}text-gray-600 hover:bg-gray-50 hover:text-gray-900{% endif %}">
                        <i class="fas fa-graduation-cap mr-3 h-6 w-6"></i>
                        Filières
                    </a>

                    <!-- Cycles -->
                    <a href="{{ path('app_cycle_index') }}" 
                       class="group flex items-center rounded-md px-2 py-2 text-sm font-medium {% if app.request.get('_route') starts with 'app_cycle' %}bg-indigo-100 text-indigo-600{% else %}text-gray-600 hover:bg-gray-50 hover:text-gray-900{% endif %}">
                        <i class="fas fa-circle-notch mr-3 h-6 w-6"></i>
                        Cycles
                    </a>

                    <!-- Années -->
                    <a href="{{ path('app_annee_index') }}" 
                       class="group flex items-center rounded-md px-2 py-2 text-sm font-medium {% if app.request.get('_route') starts with 'app_annee' %}bg-indigo-100 text-indigo-600{% else %}text-gray-600 hover:bg-gray-50 hover:text-gray-900{% endif %}">
                        <i class="fas fa-calendar-alt mr-3 h-6 w-6"></i>
                        Années
                    </a>

                    <!-- Semestres -->
                    <a href="{{ path('app_semestre_index') }}" 
                       class="group flex items-center rounded-md px-2 py-2 text-sm font-medium {% if app.request.get('_route') starts with 'app_semestre' %}bg-indigo-100 text-indigo-600{% else %}text-gray-600 hover:bg-gray-50 hover:text-gray-900{% endif %}">
                        <i class="fas fa-clock mr-3 h-6 w-6"></i>
                        Semestres
                    </a>
                </div>
            </div>

            <!-- Gestion des Salles -->
            <div class="mt-4">
                <p class="px-2 text-xs font-semibold uppercase tracking-wider text-gray-500">
                    Gestion des Salles
                </p>
                <div class="mt-2 space-y-1">
                    <!-- Salles -->
                    <a href="{{ path('app_salle_cours_index') }}" 
                       class="group flex items-center rounded-md px-2 py-2 text-sm font-medium {% if app.request.get('_route') starts with 'app_salle_cours' %}bg-indigo-100 text-indigo-600{% else %}text-gray-600 hover:bg-gray-50 hover:text-gray-900{% endif %}">
                        <i class="fas fa-door-open mr-3 h-6 w-6"></i>
                        Salles de cours
                    </a>

                    <!-- Emploi du temps -->
                    <a href="{{ path('app_emploi_temps_index') }}" 
                       class="group flex items-center rounded-md px-2 py-2 text-sm font-medium {% if app.request.get('_route') starts with 'app_emploi_temps' %}bg-indigo-100 text-indigo-600{% else %}text-gray-600 hover:bg-gray-50 hover:text-gray-900{% endif %}">
                        <i class="fas fa-calendar-week mr-3 h-6 w-6"></i>
                        Emploi du temps
                    </a>
                </div>
            </div>

            <!-- Gestion Financière -->
            <div class="mt-4">
                <p class="px-2 text-xs font-semibold uppercase tracking-wider text-gray-500">
                    Gestion Financière
                </p>
                <div class="mt-2 space-y-1">
                    <!-- Inscriptions -->
                    <a href="{{ path('app_inscription_index') }}" 
                       class="group flex items-center rounded-md px-2 py-2 text-sm font-medium {% if app.request.get('_route') starts with 'app_inscription' %}bg-indigo-100 text-indigo-600{% else %}text-gray-600 hover:bg-gray-50 hover:text-gray-900{% endif %}">
                        <i class="fas fa-user-plus mr-3 h-6 w-6"></i>
                        Inscriptions
                    </a>

                    <!-- Règlements -->
                    <a href="{{ path('app_reglement_index') }}" 
                       class="group flex items-center rounded-md px-2 py-2 text-sm font-medium {% if app.request.get('_route') starts with 'app_reglement' %}bg-indigo-100 text-indigo-600{% else %}text-gray-600 hover:bg-gray-50 hover:text-gray-900{% endif %}">
                        <i class="fas fa-money-bill-wave mr-3 h-6 w-6"></i>
                        Règlements
                    </a>
                </div>
            </div>
        </nav>
    </div>
</div>
