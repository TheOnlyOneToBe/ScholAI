<!-- Sidebar -->
<nav class="flex h-full flex-1 flex-col bg-white">
    <div class="flex flex-1 flex-col overflow-y-auto pt-5 pb-4">
        <div class="flex flex-shrink-0 items-center px-4">
            <h1 class="text-2xl font-bold text-indigo-600">Schol</h1>
        </div>
        <nav class="mt-5 flex-1 space-y-1 bg-white px-2">
            <!-- Dashboard -->
            <a href="{{ path('app_home') }}" 
               class="group flex items-center px-2 py-2 text-sm font-medium rounded-md {{ app.request.get('_route') == 'app_home' ? 'bg-indigo-100 text-indigo-600' : 'text-gray-600 hover:bg-gray-50 hover:text-gray-900' }}">
                <i class="fas fa-home mr-3 flex-shrink-0 h-6 w-6"></i>
                {{ 'nav.sidebar.home'|trans }}
            </a>

            <!-- Gestion Académique -->
            <div class="mt-4">
                <p class="px-2 text-xs font-semibold uppercase tracking-wider text-gray-500">
                    {{ 'nav.sidebar.academic_management'|trans }}
                </p>
                <div class="mt-2 space-y-1">
                    <!-- Étudiants -->
                    <a href="{{ path('app_etudiant_index') }}" 
                       class="group flex items-center px-2 py-2 text-sm font-medium rounded-md {{ app.request.get('_route') starts with 'app_etudiant' ? 'bg-indigo-100 text-indigo-600' : 'text-gray-600 hover:bg-gray-50 hover:text-gray-900' }}">
                        <i class="fas fa-user-graduate mr-3 flex-shrink-0 h-6 w-6"></i>
                        {{ 'nav.sidebar.students'|trans }}
                    </a>

                    <!-- Professeurs -->
                    <a href="{{ path('app_professeur_index') }}" 
                       class="group flex items-center px-2 py-2 text-sm font-medium rounded-md {{ app.request.get('_route') starts with 'app_professeur' ? 'bg-indigo-100 text-indigo-600' : 'text-gray-600 hover:bg-gray-50 hover:text-gray-900' }}">
                        <i class="fas fa-chalkboard-teacher mr-3 flex-shrink-0 h-6 w-6"></i>
                        {{ 'nav.sidebar.teachers'|trans }}
                    </a>

                    <!-- Notes -->
                    <a href="{{ path('app_note_index') }}" 
                       class="group flex items-center px-2 py-2 text-sm font-medium rounded-md {{ app.request.get('_route') starts with 'app_note' ? 'bg-indigo-100 text-indigo-600' : 'text-gray-600 hover:bg-gray-50 hover:text-gray-900' }}">
                        <i class="fas fa-star mr-3 flex-shrink-0 h-6 w-6"></i>
                        {{ 'nav.sidebar.grades'|trans }}
                    </a>

                    <!-- Évaluations -->
                    <a href="{{ path('app_evaluation_index') }}" 
                       class="group flex items-center px-2 py-2 text-sm font-medium rounded-md {{ app.request.get('_route') starts with 'app_evaluation' ? 'bg-indigo-100 text-indigo-600' : 'text-gray-600 hover:bg-gray-50 hover:text-gray-900' }}">
                        <i class="fas fa-tasks mr-3 flex-shrink-0 h-6 w-6"></i>
                        {{ 'nav.sidebar.evaluations'|trans }}
                    </a>

                    <!-- Matières -->
                    <a href="{{ path('app_matiere_index') }}" 
                       class="group flex items-center px-2 py-2 text-sm font-medium rounded-md {{ app.request.get('_route') starts with 'app_matiere' ? 'bg-indigo-100 text-indigo-600' : 'text-gray-600 hover:bg-gray-50 hover:text-gray-900' }}">
                        <i class="fas fa-book mr-3 flex-shrink-0 h-6 w-6"></i>
                        {{ 'nav.sidebar.subjects'|trans }}
                    </a>
                </div>
            </div>

            <!-- Organisation -->
            <div class="mt-4">
                <p class="px-2 text-xs font-semibold uppercase tracking-wider text-gray-500">
                    {{ 'nav.sidebar.organization'|trans }}
                </p>
                <div class="mt-2 space-y-1">
                    <!-- Campus -->
                    <a href="{{ path('app_campus_index') }}" 
                       class="group flex items-center px-2 py-2 text-sm font-medium rounded-md {{ app.request.get('_route') starts with 'app_campus' ? 'bg-indigo-100 text-indigo-600' : 'text-gray-600 hover:bg-gray-50 hover:text-gray-900' }}">
                        <i class="fas fa-university mr-3 flex-shrink-0 h-6 w-6"></i>
                        {{ 'nav.sidebar.campus'|trans }}
                    </a>

                    <!-- Départements -->
                    <a href="{{ path('app_departement_index') }}" 
                       class="group flex items-center px-2 py-2 text-sm font-medium rounded-md {{ app.request.get('_route') starts with 'app_departement' ? 'bg-indigo-100 text-indigo-600' : 'text-gray-600 hover:bg-gray-50 hover:text-gray-900' }}">
                        <i class="fas fa-building mr-3 flex-shrink-0 h-6 w-6"></i>
                        {{ 'nav.sidebar.departments'|trans }}
                    </a>

                    <!-- Filières -->
                    <a href="{{ path('app_filiere_index') }}" 
                       class="group flex items-center px-2 py-2 text-sm font-medium rounded-md {{ app.request.get('_route') starts with 'app_filiere' ? 'bg-indigo-100 text-indigo-600' : 'text-gray-600 hover:bg-gray-50 hover:text-gray-900' }}">
                        <i class="fas fa-graduation-cap mr-3 flex-shrink-0 h-6 w-6"></i>
                        {{ 'nav.sidebar.curricula'|trans }}
                    </a>

                    <!-- Cycles -->
                    <a href="{{ path('app_cycle_index') }}" 
                       class="group flex items-center px-2 py-2 text-sm font-medium rounded-md {{ app.request.get('_route') starts with 'app_cycle' ? 'bg-indigo-100 text-indigo-600' : 'text-gray-600 hover:bg-gray-50 hover:text-gray-900' }}">
                        <i class="fas fa-layer-group mr-3 flex-shrink-0 h-6 w-6"></i>
                        {{ 'nav.sidebar.cycles'|trans }}
                    </a>

                    <!-- Années -->
                    <a href="{{ path('app_annee_index') }}" 
                       class="group flex items-center px-2 py-2 text-sm font-medium rounded-md {{ app.request.get('_route') starts with 'app_annee' ? 'bg-indigo-100 text-indigo-600' : 'text-gray-600 hover:bg-gray-50 hover:text-gray-900' }}">
                        <i class="fas fa-calendar-alt mr-3 flex-shrink-0 h-6 w-6"></i>
                        {{ 'nav.sidebar.school_years'|trans }}
                    </a>

                    <!-- Semestres -->
                    <a href="{{ path('app_semestre_index') }}" 
                       class="group flex items-center px-2 py-2 text-sm font-medium rounded-md {{ app.request.get('_route') starts with 'app_semestre' ? 'bg-indigo-100 text-indigo-600' : 'text-gray-600 hover:bg-gray-50 hover:text-gray-900' }}">
                        <i class="fas fa-clock mr-3 flex-shrink-0 h-6 w-6"></i>
                        {{ 'nav.sidebar.semesters'|trans }}
                    </a>
                </div>
            </div>

            <!-- Gestion des Salles -->
            <div class="mt-4">
                <p class="px-2 text-xs font-semibold uppercase tracking-wider text-gray-500">
                    {{ 'nav.sidebar.classroom_management'|trans }}
                </p>
                <div class="mt-2 space-y-1">
                    <!-- Salles -->
                    <a href="{{ path('app_salle_cours_index') }}" 
                       class="group flex items-center px-2 py-2 text-sm font-medium rounded-md {{ app.request.get('_route') starts with 'app_salle_cours' ? 'bg-indigo-100 text-indigo-600' : 'text-gray-600 hover:bg-gray-50 hover:text-gray-900' }}">
                        <i class="fas fa-door-open mr-3 flex-shrink-0 h-6 w-6"></i>
                        {{ 'nav.sidebar.classrooms'|trans }}
                    </a>

                    <!-- Emploi du temps -->
                    <a href="{{ path('app_emploi_temps_index') }}" 
                       class="group flex items-center px-2 py-2 text-sm font-medium rounded-md {{ app.request.get('_route') starts with 'app_emploi_temps' ? 'bg-indigo-100 text-indigo-600' : 'text-gray-600 hover:bg-gray-50 hover:text-gray-900' }}">
                        <i class="fas fa-calendar-week mr-3 flex-shrink-0 h-6 w-6"></i>
                        {{ 'nav.sidebar.timetable'|trans }}
                    </a>
                </div>
            </div>

            <!-- Gestion Financière -->
            <div class="mt-4">
                <p class="px-2 text-xs font-semibold uppercase tracking-wider text-gray-500">
                    {{ 'nav.sidebar.financial_management'|trans }}
                </p>
                <div class="mt-2 space-y-1">
                    <!-- Inscriptions -->
                    <a href="{{ path('app_inscription_index') }}" 
                       class="group flex items-center px-2 py-2 text-sm font-medium rounded-md {{ app.request.get('_route') starts with 'app_inscription' ? 'bg-indigo-100 text-indigo-600' : 'text-gray-600 hover:bg-gray-50 hover:text-gray-900' }}">
                        <i class="fas fa-user-plus mr-3 flex-shrink-0 h-6 w-6"></i>
                        {{ 'nav.sidebar.enrollments'|trans }}
                    </a>

                    <!-- Règlements -->
                    <a href="{{ path('app_reglement_index') }}" 
                       class="group flex items-center px-2 py-2 text-sm font-medium rounded-md {{ app.request.get('_route') starts with 'app_reglement' ? 'bg-indigo-100 text-indigo-600' : 'text-gray-600 hover:bg-gray-50 hover:text-gray-900' }}">
                        <i class="fas fa-money-bill-wave mr-3 flex-shrink-0 h-6 w-6"></i>
                        {{ 'nav.sidebar.payments'|trans }}
                    </a>
                </div>
            </div>
        </nav>
    </div>
</nav>
