{% extends 'base.html.twig' %}

{% block title %}Annee{% endblock %}

{% block body %}
    <h1>Annee</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ annee.id }}</td>
            </tr>
            <tr>
                <th>Year_start</th>
                <td>{{ annee.yearStart ? annee.yearStart|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>YearEnd</th>
                <td>{{ annee.yearEnd ? annee.yearEnd|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>YearStatut</th>
                <td>{{ annee.yearStatut ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_annee_index') }}">back to list</a>

    <a href="{{ path('app_annee_edit', {'id': annee.id}) }}">edit</a>

    {{ include('annee/_delete_form.html.twig') }}
{% endblock %}
