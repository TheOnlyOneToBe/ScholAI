{% extends 'base.html.twig' %}

{% block title %}PayementMethod{% endblock %}

{% block body %}
    <h1>PayementMethod</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ payement_method.id }}</td>
            </tr>
            <tr>
                <th>PayementName</th>
                <td>{{ payement_method.payementName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_payement_method_index') }}">back to list</a>

    <a href="{{ path('app_payement_method_edit', {'id': payement_method.id}) }}">edit</a>

    {{ include('payement_method/_delete_form.html.twig') }}
{% endblock %}
