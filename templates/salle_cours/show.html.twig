{% extends 'base.html.twig' %}

{% block title %}SalleCours{% endblock %}

{% block body %}
    <h1>SalleCours</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ salle_cour.id }}</td>
            </tr>
            <tr>
                <th>NomSalle</th>
                <td>{{ salle_cour.NomSalle }}</td>
            </tr>
            <tr>
                <th>Capacite</th>
                <td>{{ salle_cour.capacite }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_salle_cours_index') }}">back to list</a>

    <a href="{{ path('app_salle_cours_edit', {'id': salle_cour.id}) }}">edit</a>

    {{ include('salle_cours/_delete_form.html.twig') }}
{% endblock %}
