<style>
    /* Navbar styles */
    .navbar {
        @apply bg-white border-b border-gray-200 fixed w-full z-30 top-0 shadow-sm;
    }

    .navbar-container {
        @apply px-3 py-2.5 lg:px-5 lg:pl-3;
    }

    .navbar-toggle-button {
        @apply mr-2 text-gray-600 hover:text-indigo-600 cursor-pointer p-2 hover:bg-indigo-50 focus:bg-indigo-50 focus:ring-2 focus:ring-indigo-200 rounded-lg transition-all duration-200;
    }

    .navbar-brand {
        @apply text-xl font-bold flex items-center lg:ml-2.5;
    }

    .navbar-search {
        @apply relative lg:ml-64 md:w-96;
    }

    .navbar-search-icon {
        @apply absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none text-gray-400;
    }

    .navbar-search-input {
        @apply bg-gray-50 border border-gray-300 text-gray-700 text-sm rounded-lg focus:ring-indigo-500 focus:border-indigo-500 block w-full pl-10 p-2.5 transition-all duration-200;
    }

    .navbar-notification {
        @apply relative p-2 text-gray-500 rounded-lg hover:text-indigo-600 hover:bg-indigo-50 transition-all duration-200;
    }

    .notification-badge {
        @apply absolute inline-flex items-center justify-center w-5 h-5 text-xs font-bold text-white bg-red-500 rounded-full -top-1 -right-1;
    }

    .navbar-profile-container {
        @apply relative ml-3;
    }

    .navbar-profile-button {
        @apply flex items-center space-x-4 p-2 hover:bg-indigo-50 rounded-lg transition-all duration-200;
    }

    .navbar-profile-image {
        @apply h-8 w-8 rounded-full ring-2 ring-indigo-100;
    }

    .navbar-profile-name {
        @apply text-sm font-medium text-gray-900;
    }

    .navbar-profile-role {
        @apply text-sm text-gray-500;
    }

    .navbar-profile-arrow {
        @apply -mr-1 ml-2 h-5 w-5 text-gray-400;
    }

    .navbar-dropdown {
        @apply absolute right-0 mt-2 w-48 rounded-lg shadow-lg py-1 bg-white ring-1 ring-black ring-opacity-5 focus:outline-none;
        transform-origin: top right;
    }

    .navbar-dropdown-item {
        @apply block px-4 py-2 text-sm text-gray-700 hover:bg-indigo-50 transition-all duration-200;
    }
</style>
